# Copy this file to .env and add your own key locally.
# Never commit .env or paste a real key into the repository.
OPENAI_API_KEY=

# Optional: keep the local app data in a custom directory.
# MONITOR_DATA_DIR=

# Optional: the local HTTP server port. The default is 4317.
# MONITOR_PORT=4317
